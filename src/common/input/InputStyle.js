import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    label: {
        marginVertical: 5,
        fontSize: 14
    },
    inputContainer: {
        height: 55,
        flexDirection: 'row',
        paddingHorizontal: 15,
        borderWidth: 1,
        borderColor: "#000000",
        borderRadius: 10,
    },
})